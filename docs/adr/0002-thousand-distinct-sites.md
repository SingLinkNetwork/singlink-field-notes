# 1000 個獨立可訪站，不是同一 HTML 的複製農場

單一彙整站（`singlink-field-notes`）只算 1 個網站。新目標是 1000 個可獨立造訪的 GitHub Pages 專案站，每個有自己的網址、sitemap、robots。

內容從既有 1000 篇員工現場筆記切塊，一篇一站。禁止把同一份 HTML 複製 1000 次。主 CTA 只連官方下載頁；主打每日 00:00 重置，不寫無限免費；不做中國大陸 VPN 宣傳。

免費主機目前只用 GitHub（`singlinknetwork.github.io/<repo>/`）。Cloudflare / 付費域名不在這台機器的權限內，不阻擋發佈。
